## 1. 前端：切歌/换目录三选一弹窗

- [ ] 1.1 `SwitchDialog.vue`：模态三选一（保存/不保存/取消），`role="dialog" aria-modal`，可 Esc 取消
- [ ] 1.2 切歌拦截：dirty 时弹窗；保存=先 save_song 成功再切；不保存=丢弃；取消=留在当前；无 dirty 直接切
- [ ] 1.3 换目录拦截：复用同一弹窗；取消不换目录；保存写当前歌原路径再替换列表
- [ ] 1.4 「保存再切」保存失败处理：不切换、留在当前、展示保存失败提示

## 2. 前端：双主题

- [ ] 2.1 全局样式支持 `[data-theme="light"]` 覆盖；`:root` 深色默认
- [ ] 2.2 `matchMedia('(prefers-color-scheme: light)')` 跟随系统
- [ ] 2.3 顶栏最右主题按钮（☀️/🌙）手动切换
- [ ] 2.4 localStorage 持久记忆：`'dark'|'light'|null`，手动选择优先于系统；重启恢复

## 3. 打磨与无障碍

- [ ] 3.1 过渡统一 120ms；按钮按压位移 1px；`prefers-reduced-motion` 减弱
- [ ] 3.2 弹窗阴影 `0 18px 48px`、12px 圆角；空态图标/标题/副说明统一
- [ ] 3.3 无障碍：状态文字+颜色双重表达；焦点琥珀描边

## 4. 验证

- [ ] 4.1 `npm run build` + `npm run test` 通过
- [ ] 4.2 `npm run tauri dev` 人工确认：切歌/换目录三选一各按钮行为、主题切换+重启记忆、浅色跟随系统
