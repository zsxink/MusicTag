# Node / Vite
node_modules/
dist/
*.local

# Rust / Tauri
src-tauri/target/
src-tauri/gen/schemas/

# 构建产物
*.log

# 本地 codegraph 索引（SQLite 二进制，随代码变更重建；可随时 `codegraph index` 重建）
.codegraph/

# pipe-core 运行态（版本控制外）：节点级状态文件 + epic 并行状态 + git worktree 隔离目录
.agents/runs/
.worktrees/

# macOS / Finder 元数据
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes
.fseventsd
