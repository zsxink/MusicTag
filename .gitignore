# Node / Vite
node_modules/
dist/
*.local

# Rust / Tauri
src-tauri/target/
src-tauri/gen/schemas/

# 构建产物
*.log

# 本地 codegraph 索引（SQLite 二进制，随代码变更重建；可随时 `codegraph index` 重建）
.codegraph/

# macOS / Finder 元数据
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes
.fseventsd
